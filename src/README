Hi,

I have implemented an object called PokerHand that has a 
compareTo method (similar to how a priority queue in java works).

The program can be started by running MyClient.java

If I make the PokerHand implement Comparable, then it can 
be used with a PriorityQueue.
